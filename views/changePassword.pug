extends layouts/main.pug

block content
    .form-container
        form(action=routes.users+routes.changePassword, method="post", onsubmit="checkSubmit()").containPassword
            input(type="password", placeholder="Current Password", name="currentPassword", required=true)#currentPassword
            input(type="text", placeholder="New Password", name="password", required=true)
            input(type="password", placeholder="Verify New Password", name="password2", required=true)
            .verify-password
                if changePasswordFail
                    #changePasswordFailed Please check your current password.
            input(type="submit", value="Change Password")
    script
        include ../frontJS/passwordMatch.js
    script.
        const changePasswordFailed = document.getElementById("changePasswordFailed");
        setTimeout(function(){
            changePasswordFailed.style.opacity = 0;
        }, 3500);